<configuration>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="ScheduleBot"/>

    <!-- ===== КОНСОЛЬ ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===== ФАЙЛ с ротацией по дате + размеру ===== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <!-- текущий активный файл -->
        <file>/app/logs/ScheduleBot.log</file>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <!-- имя архивного файла: app.2025-07-14.1.log.gz -->
            <fileNamePattern>/app/logs/ScheduleBot.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>

            <!-- ротация по размеру одного файла -->
            <maxFileSize>3GB</maxFileSize>

            <!-- сколько дней хранить -->
            <maxHistory>120</maxHistory>

            <!-- максимум всех логов суммарно -->
            <totalSizeCap>500GB</totalSizeCap>

        </rollingPolicy>
    </appender>

    <!-- ===== ОТДЕЛЬНЫЙ ФАЙЛ только для ERROR ===== -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/app/logs/error.log</file>

        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>/app/logs/error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>1GB</maxFileSize>
            <maxHistory>365</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- ===== УРОВНИ ===== -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

    <logger name="org.example" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </logger>

</configuration>
